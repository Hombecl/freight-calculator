// FbaPage - imports the main calculator for FBA size tier mode
import Calculator from '../components/Calculator';

export default function FbaPage() {
  return <Calculator fixedMode="fba" hideHeader={true} />;
}
